#!/usr/bin/env node

/**
 * Health Check Script
 *
 * Makes an HTTP request to the service's health endpoint.
 * Exits with 0 if healthy, 1 if unhealthy.
 */

import { healthcheck } from "@saf/node-express";

healthcheck();
